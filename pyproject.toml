[project]
name = "smart-college-canteen"
version = "1.0.0"
requires-python = ">=3.9"
dependencies = [
	"fastapi",
	"uvicorn[standard]",
	"sqlalchemy",
	"pydantic[email]",
	"python-jose[cryptography]",
	"qrcode[pil]",
	"psycopg2-binary",
]

[tool.vercel]
entrypoint = "backend.main:app"